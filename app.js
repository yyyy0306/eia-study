const D=window.EIA_DATA;const $=id=>document.getElementById(id);const storeKey="eia-study-state-v2";let state=JSON.parse(localStorage.getItem(storeKey)||'{"favorite":[],"done":[],"weak":[]}');state.favorite=new Set(state.favorite||[]);state.done=new Set(state.done||[]);state.weak=new Set(state.weak||[]);let subject="全部",status="全部",view="chapters",chapterIndex=0,currentQuiz=null,showAnswer=false;function save(){localStorage.setItem(storeKey,JSON.stringify({favorite:[...state.favorite],done:[...state.done],weak:[...state.weak]}))}function esc(s){return String(s).replace(/[&<>"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;"}[m]))}function topicText(t){return [t.subject,t.level,t.title,t.summary,t.mistake,t.exam,...t.tags].join(" ").toLowerCase()}function filteredTopics(){const kw=$("searchInput").value.trim().toLowerCase();return D.topics.filter(t=>{const s=subject==="全部"||t.subject===subject;const st=status==="全部"||t.level===status||(status==="收藏"&&state.favorite.has(t.id))||(status==="薄弱"&&state.weak.has(t.id))||(status==="掌握"&&state.done.has(t.id));return s&&st&&(!kw||topicText(t).includes(kw))})}function renderControls(){$("subjectSelect").innerHTML=D.subjects.map(s=>`<option value="${s}">${s}</option>`).join("");$("subjectSelect").value=subject;document.querySelectorAll(".tab").forEach(b=>b.classList.toggle("active",b.dataset.view===view));$("statusSelect").value=status}function renderStats(){$("topicCount").textContent=D.topics.length;$("questionCount").textContent=D.questions.length;$("doneCount").textContent=state.done.size;$("weakCount").textContent=state.weak.size;const radar=D.subjects.filter(s=>s!=="全部").map(s=>{const all=D.topics.filter(t=>t.subject===s);const high=all.filter(t=>t.level==="高频").length;const pct=Math.round(high/all.length*100);return `<div class="radar-row"><strong>${s}</strong><div class="track"><i style="width:${pct}%"></i></div><span>${high} 高频</span></div>`}).join("");$("radar").innerHTML=radar;$("wordCloud").innerHTML=D.signals.map(w=>`<button data-word="${w}">${w}</button>`).join("");const weak=D.topics.filter(t=>state.weak.has(t.id)).slice(0,2);const plan=weak.length?weak.map(t=>`复盘薄弱点：${t.title}`):["先学总纲、大气、水环境三章","随机练习 5 题，不会就标薄弱"];plan.push(`今日章节：${D.chapters[chapterIndex].title}`);$("todayPlan").innerHTML=plan.map(x=>`<li>${x}</li>`).join("")}function renderChapters(){const nav=D.chapters.map((c,i)=>`<button class="chapter-link ${i===chapterIndex?"active":""}" data-chapter="${i}"><strong>${c.title}</strong><span>${c.subject} · ${c.lessons.length} 个知识点 · ${c.questions.length} 道练习</span></button>`).join("");$("chapterNav").innerHTML=nav;const c=D.chapters[chapterIndex];$("chapterMain").innerHTML=`<div class="badge-row"><span class="badge">${c.subject}</span><span class="badge high">章节学习</span></div><h2>${c.title}</h2><p class="chapter-subtitle">${c.focus}</p><div class="lesson-grid">${c.lessons.map(l=>`<section class="lesson-card"><h3>${l[0]}</h3><p>${l[1]}</p></section>`).join("")}</div><h2>章节练习</h2><div class="practice-list">${c.questions.map((q,i)=>`<section class="practice-card"><h3>${i+1}. ${q[0]}</h3><ol class="options" type="A">${q[1].map(o=>`<li>${o}</li>`).join("")}</ol><details><summary>查看答案解析</summary><p><strong>答案：${q[2]}</strong>。${q[3]}</p></details></section>`).join("")}</div>`}function renderCards(){const list=filteredTopics();$("resultText").textContent=`当前筛选出 ${list.length} 个知识点`;$("cards").innerHTML=list.map(t=>`<article class="topic-card"><div class="topic-head"><h2>${t.title}</h2><button class="star ${state.favorite.has(t.id)?"active":""}" data-act="favorite" data-id="${t.id}">★</button></div><div class="badge-row"><span class="badge">${t.subject}</span><span class="badge ${t.level==="高频"?"high":"mid"}">${t.level}</span>${state.done.has(t.id)?'<span class="badge low">掌握</span>':''}${state.weak.has(t.id)?'<span class="badge high">薄弱</span>':''}</div><p>${t.summary}</p><div class="topic-detail"><strong>易错：</strong>${t.mistake}<br><strong>出题：</strong>${t.exam}</div><div class="tags">${t.tags.map(tag=>`<span class="tag">${tag}</span>`).join("")}</div><div class="card-actions"><button class="btn ${state.done.has(t.id)?"active-done":""}" data-act="done" data-id="${t.id}">掌握</button><button class="btn weak ${state.weak.has(t.id)?"active-weak":""}" data-act="weak" data-id="${t.id}">薄弱</button></div></article>`).join("")}function renderQuiz(){if(!currentQuiz)currentQuiz=D.questions[Math.floor(Math.random()*D.questions.length)];$("quizBox").innerHTML=`<div class="badge-row"><span class="badge">${currentQuiz.subject}</span><span class="badge">${currentQuiz.chapter}</span></div><h3>${currentQuiz.text}</h3><ol class="options" type="A">${currentQuiz.options.map(o=>`<li>${o}</li>`).join("")}</ol><div class="answer ${showAnswer?"open":""}"><p><strong>答案：${currentQuiz.answer}</strong>。${currentQuiz.explain}</p></div>`}function renderProgress(){const rows=D.subjects.filter(s=>s!=="全部").map(s=>{const all=D.topics.filter(t=>t.subject===s);const done=all.filter(t=>state.done.has(t.id)).length;const pct=Math.round(done/all.length*100);return `<div class="progress-row"><strong>${s}</strong><div class="meter"><i style="width:${pct}%"></i></div><span>${done}/${all.length}</span></div>`}).join("");$("progressList").innerHTML=rows}function render(){renderControls();renderStats();renderChapters();renderCards();renderQuiz();renderProgress();document.querySelectorAll(".view").forEach(v=>v.classList.remove("active"));$(view+"View").classList.add("active")}function toggle(type,id){const s=state[type];s.has(id)?s.delete(id):s.add(id);if(type==="done"&&s.has(id))state.weak.delete(id);if(type==="weak"&&s.has(id))state.done.delete(id);save();render()}$("searchInput").addEventListener("input",render);$("subjectSelect").addEventListener("change",e=>{subject=e.target.value;render()});$("statusSelect").addEventListener("change",e=>{status=e.target.value;render()});$("tabs").addEventListener("click",e=>{const b=e.target.closest("[data-view]");if(b){view=b.dataset.view;render()}});$("chapterNav").addEventListener("click",e=>{const b=e.target.closest("[data-chapter]");if(b){chapterIndex=Number(b.dataset.chapter);render()}});$("cards").addEventListener("click",e=>{const b=e.target.closest("[data-act]");if(!b)return;toggle(b.dataset.act,b.dataset.id)});$("wordCloud").addEventListener("click",e=>{const b=e.target.closest("[data-word]");if(b){$("searchInput").value=b.dataset.word;view="library";render()}});$("drawBtn").addEventListener("click",()=>{currentQuiz=D.questions[Math.floor(Math.random()*D.questions.length)];showAnswer=false;render()});$("answerBtn").addEventListener("click",()=>{showAnswer=!showAnswer;renderQuiz()});$("markWeakBtn").addEventListener("click",()=>{if(currentQuiz){const related=D.topics.find(t=>t.subject===currentQuiz.subject);if(related)toggle("weak",related.id)}});$("themeBtn").addEventListener("click",()=>{const app=document.querySelector(".app");app.dataset.theme=app.dataset.theme==="dark"?"light":"dark";localStorage.setItem("eia-theme",app.dataset.theme)});$("exportBtn").addEventListener("click",()=>{const blob=new Blob([JSON.stringify({favorite:[...state.favorite],done:[...state.done],weak:[...state.weak]},null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="环评复习进度.json";a.click();URL.revokeObjectURL(a.href)});$("importFile").addEventListener("change",async e=>{const f=e.target.files[0];if(!f)return;const d=JSON.parse(await f.text());state.favorite=new Set(d.favorite||[]);state.done=new Set(d.done||[]);state.weak=new Set(d.weak||[]);save();render();e.target.value=""});document.querySelector(".app").dataset.theme=localStorage.getItem("eia-theme")||"light";render();
